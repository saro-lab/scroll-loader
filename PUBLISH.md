# build & publish
1. yarn build
    ``` shell
    yarn build
    ```
2. copy type file
    windows
    ``` shell
    copy /Y /B dist\index.d.ts .
    ```
3. publish
   ``` shell
   yarn publish
   ```